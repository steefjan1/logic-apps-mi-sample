# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: logic-apps-mi-sample
metadata:
  template: logic-apps-mi-sample
infra:
  provider: bicep
  path: infra
services:
  workflows:
    project: ./workflow-app
    host: function
    language: js
